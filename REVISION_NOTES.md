# DWG to ESP300 Lab Code Package — 개정 내역

개정일: 2026-08-17
대상 패키지: DWG_to_ESP300_Lab_Code_Package_2026-08-16
변경 파일: `visualizer_server.py`, `dwg_to_esp300.py`, `visualizer/index.html`, `visualizer/app.js`, `visualizer/styles.css`

## 1. 개요

기존 패키지는 DWG/DXF 도면을 ESP300 스테이지 제어 코드로 변환하고 미리보기하는 기능까지 제공하였다. 본 개정에서는 브라우저 인터페이스에서 ESP300을 직접 수동 조작(조그, joystick jog)할 수 있는 기능을 추가하고, 시리얼 통신 안정성 및 다중 패스 가공 관련 사항을 보완하였다.

## 2. 신규 기능

### 2.1 수동 조그(Joystick) 제어 패널

시각화 인터페이스(`visualizer/`)에 실기계 수동 이동 제어 패널을 신설하였다. 백엔드는 `visualizer_server.py`의 `JogController` 클래스가 담당하며, ESP300과의 시리얼 통신은 기존 생성 스크립트와 동일한 명령 체계(VA, PA, MO, MF, TP, AB)를 사용한다.

| 기능 | 설명 |
|---|---|
| 연결(Connect) | COM 포트, 통신 속도, X/Y 및 Z 소프트 리밋 범위 지정. Dry run(가상 실행) 모드 기본 적용 |
| 축별 조그 이동 | X, Y, Z 축 개별 이동. 이동 단위 0.001mm(1μm)~5mm 중 선택 가능. 버튼 길게 누름으로 연속 이동, 포인터 해제 시 즉시 정지 |
| 키보드 단축키 | 방향키(X/Y), Page Up/Down(Z), Space(비상정지) |
| 비상정지(E-STOP) | 전축 정지 및 모터 비활성화 명령 전송, 진행 중인 자동 실행도 함께 중단. 재사용 시 재연결 필요 |
| 원점 이동(Home) | 소프트웨어 기준 원점(0,0,0)으로 절대 이동 |
| 로컬 원점 설정 | 현재 위치를 임의 좌표(통상 도면 중심)로 라벨링. **ESP300 자체의 절대 좌표계는 변경하지 않으며, 물리적 이동도 발생하지 않는다.** 소프트웨어 계층에서만 좌표 오프셋을 관리한다 |
| 좌표 직접 이동(Go to) | X/Y/Z 좌표 직접 입력 후 절대 이동, 소프트 리밋 적용 |
| 계획 경로 실행(Run on machine) | 화면에 표시된 가공 계획(경로 계획)을 실기계가 순차 실행. 실행 진행 상황에 따라 화면 재생 위치가 동기화된다 |
| 실시간 위치 표시 | 조그 조작 또는 외부 조작으로 발생한 실제 스테이지 이동이 3차원 미리보기 및 좌표 표시에 실시간 반영된다 |

안전 관련 사항: X/Y 이동은 원형 작업영역 반경으로, Z 이동은 지정 범위로 제한된다. 단일 조그 이동 최대 거리는 5mm로 고정되어 있다. 원점 이동, 좌표 직접 이동 등 상대적으로 큰 이동을 수반하는 동작은 실행 전 확인 절차를 거친다.

### 2.2 다중 패스 가공 시 Z축 보정

동일 경로를 다중 패스(pass)로 반복 가공할 경우, 2패스마다 초점(focus) Z 위치를 디포커스 방향으로 1μm(0.001mm)씩 후퇴시키는 로직을 추가하였다. 이는 반복 가공에 따른 가공면 침하를 보정하기 위한 것이다. 경로가 전환되면 해당 보정값은 자동으로 초기화된다.

적용 범위: 생성되는 독립 실행 스크립트(`dwg_to_esp300.py`의 `generate_esp300_script` 출력) 및 시각화 인터페이스의 경로 계획·실기계 실행 기능(`visualizer_server.py`) 양쪽 모두.

### 2.3 도면 배치 회전(45도)

원형 작업영역(지름 25mm)에 도면을 맞춰 넣을 때, 도면을 자체 중심 기준 45도 회전한 뒤 배율을 계산하도록 변경하였다. 정사각형이 아닌 배치의 경우, 회전 적용 시 축 정렬 배치 대비 더 큰 배율로 작업영역에 수용할 수 있다. 기본값은 45도이며, 0으로 설정 시 비활성화된다.

적용 범위: CLI 옵션(`--rotate-deg`, `dwg_to_esp300.py`) 및 브라우저 인터페이스(툴바 "Rotate (deg)" 입력란).

## 3. 결함 수정

### 3.1 시리얼 응답 처리 오류

위치 조회(TP) 명령에 대한 응답을 수신 완료로 판단하는 기준이 불명확하여, 응답이 통신 구간에서 분할 수신되는 경우 이전 응답의 잔여 바이트가 다음 응답에 혼입되는 결함이 있었다. 이로 인해 실제 위치값이 정상 범위를 크게 벗어난 값(예: 400mm 이상)으로 오표시되는 현상이 발생하였다.

조치: 응답 종결 문자(캐리지리턴/개행)를 확인한 후 응답 완료로 판단하도록 변경하였으며, 파싱된 값이 축 이동 범위를 초과하는 경우(250mm 초과) 해당 값을 폐기하고 재조회하도록 방어 로직을 추가하였다.

### 3.2 원점 이동 기능 오동작

`JogController` 클래스 내부에서 속성명과 메서드명이 중복되어(`home`) 원점 이동 기능 호출 시 오류가 발생하던 결함을 수정하였다.

### 3.3 인터페이스 레이아웃 오류

조그 패널 추가로 인해 우측 패널의 그리드 레이아웃 정의가 실제 구성요소 수와 불일치하여 하위 요소(도형 편집, 경로 목록)가 정상적으로 선택되지 않던 문제를 수정하였다.

## 4. 참고 및 제한사항

- **Python 버전 호환성**: `visualizer_server.py`는 `cgi` 모듈을 사용하며, 이는 Python 3.13부터 표준 라이브러리에서 제거되었다. Python 3.12 이하 환경에서 실행할 것을 권장한다.
- **전원 관리**: 실행 환경(PC)이 절전 모드로 전환되는 경우 USB 시리얼 연결이 끊기거나 포트 번호가 변경될 수 있다. 조그 조작 또는 자동 실행 중에는 운영체제 절전 설정을 비활성화할 것을 권장한다.
- DWG 파일 변환을 위하여 `aspose-cad` 패키지를 추가 설치하였다(ODA File Converter 미설치 환경 대응).
